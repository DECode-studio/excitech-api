const app = require('../app');
const port = process.env.PORT || 3003 || 5000 || 8000

app.get('/', (_, res) => {
    return res.send('Excitech ID API Server')
  });

app.listen(port, () => {
  console.log(`Server started on http://localhost:${port}`);
});